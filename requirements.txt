# Direct dependencies
flask==3.1.3
gunicorn==26.0.0
requests==2.34.2
beautifulsoup4==4.15.0

# Transitive dependencies, pinned so a container recreation installs the exact
# same tree as the last known-good one (the compose file installs at container
# start — an assumed choice, see audit P8 in PR #15). Kept current by Dependabot.
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
idna==3.18
itsdangerous==2.2.0
jinja2==3.1.6
markupsafe==3.0.3
packaging==26.2
soupsieve==2.8.4
typing_extensions==4.16.0
urllib3==2.7.0
werkzeug==3.1.8
